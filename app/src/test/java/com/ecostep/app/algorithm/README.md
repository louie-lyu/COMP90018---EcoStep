# algorithm (unit tests)

Owner: **Duo Lyu** (Algorithms, Carbon Calculation, EcoPoints and AI Personalisation module).

Unit tests for `CarbonCalculator`, `EcoPointsCalculator`, `TransportClassifier`,
`MissionGenerator` and `MissionValidator` — see the interfaces under
[`main/.../algorithm/`](../../../../../../main/java/com/ecostep/app/algorithm/CarbonCalculator.kt).
For a working example of the test setup (JUnit + kotlinx.serialization), see
[`JourneySummaryTest.kt`](../data/model/JourneySummaryTest.kt).

Due dates and evidence: see `docs/WORK_PLAN.md` at the repo root ("Unit Tests" row, due 27 Sep).

Delete this file once real test files exist in this package.
